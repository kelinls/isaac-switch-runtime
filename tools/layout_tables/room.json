{
  "schema": "isaac-layout-table/1",
  "build_id": "91C73FDD575061318D68886316AFEAC72388B2AB",
  "struct": "Room",
  "source_table": "room",
  "note": [
    "Room 目前只登记**已经拿到硬证据**的字段：引擎自己的 Room::WorldToScreenPosition 里就把滚动偏移",
    "当成 Room + 0x1938 用（先 mov w8, #0x1938 再 add x1, x19, x8，把该字段的**地址**交给",
    "Vector2::operator+）—— 这是「取地址传参」形态，不是 ldr [x19,#…]。",
    "confidence 沿用 room_descriptor.json 的口径：confirmed 才允许对外暴露成 API。"
  ],
  "rows": [
    {
      "name": "RenderScrollOffset",
      "kind": "vector2",
      "offset": 6456,
      "width": 16,
      "confidence": "confirmed",
      "semantic": "房间渲染的滚动偏移向量（PC 侧 Room:GetRenderScrollOffset()）。世界坐标转屏幕坐标时先加它、再加 Game 的渲染位置",
      "evidence": [
        {
          "function": "_ZNK15IsaacRepentance4Room21WorldToScreenPositionEN4KAGE4Math7Vector2E",
          "address": "0x0048937c",
          "offset": 6456,
          "width": 0,
          "why": "该函数把 Room + 0x1938 的地址传给 Vector2::operator+（前一条指令 mov w8, #0x1938 给出偏移）⇒ 这个字段就是房间的渲染滚动偏移；紧随其后又加上 Game + 0x24F9B0 的向量才得到屏幕坐标"
        }
      ]
    },
    {
      "name": "EnterFrameCount",
      "kind": "int32",
      "offset": 6428,
      "width": 4,
      "confidence": "confirmed",
      "semantic": "进入房间那一刻的本局帧号：引擎自己的 `Room::GetFrameCount()` 返回的就是『`Game` 的本局帧计数 − 本字段』，所以它是『本房间是在第几帧开始的』；离开/重进房间时被写成当时的帧计数（于是 `GetFrameCount()` 归零）",
      "evidence": [
        {
          "kind": "access",
          "function": "_ZN15IsaacRepentance4Room13GetFrameCountEv",
          "address": "0x00470b2c",
          "offset": 6428,
          "width": 4,
          "why": "函数体内唯一一处 `this` 字段访问：`ldr w9, [x0, #0x191c]`，紧接着 0x470B30 `sub w0, w8, w9` 返回『本局帧计数 − 本字段』"
        },
        {
          "kind": "semantic",
          "function": "_ZN15IsaacRepentance4Room13GetFrameCountEv",
          "address": "0x00470b20",
          "immediate": 63900,
          "why": "被减数的来源：`mov w9, #0xf99c` + 0x470B24 `movk w9, #0x24, lsl #16` = **0x24F99C**（`Game` 的本局帧计数，本仓库已有独立记录），再经 0x470B14/0x470B18 取 `g_Game` 槽、0x470B1C 解一层得到 `Game*` ⇒ 本字段是『进房间时的帧号』（不是帧数）。另：入口 0x470B0C 先 `ldrb w8, [x0]` + `cbz`，房间不活动时该函数直接返回 −1"
        }
      ]
    },
    {
      "name": "GetFrameCount",
      "kind": "function_entry",
      "offset": 4655884,
      "width": 0,
      "confidence": "candidate",
      "semantic": "★ 本行的 offset 是**函数入口偏移**（0x470B0C，符号 `_ZN15IsaacRepentance4Room13GetFrameCountEv`），不是结构体字段偏移 —— 它对应 `runtime_constants.hpp` 里的 `kRoomGetFrameCountOffset`（EID 的 `game:GetRoom():GetFrameCount()` 用它；缺它时背包合成那条渲染路径每帧抛错）。⚠️ confidence 取 candidate 的理由与其他行不同：**命名没有疑问**（符号名就是它），但『入口地址 = 0x470B0C』这一项本工具核不到（表机制核的是指令里的偏移/立即数），入口由符号表给出（`tools/nro_symbols.py` 读到同一个地址，宿主侧另有 16 字节入口指纹）⇒ 只登记、不进对外 API。取到的那个字段见本表 `EnterFrameCount` 行",
      "evidence": [
        {
          "kind": "access",
          "function": "_ZN15IsaacRepentance4Room13GetFrameCountEv",
          "address": "0x00470b2c",
          "offset": 6428,
          "width": 4,
          "why": "函数体内的真实访存：`ldr w9, [x0, #0x191c]`（该字段的证据另见 `EnterFrameCount` 行）；入口 0x470B0C 起是 `ldrb w8, [x0]` / `cbz` / 取 `g_Game` 槽 / 相减 / `ret`，活跃路径一共 11 条指令，房间不活动时跳到 0x470B38 直接返回 −1"
        },
        {
          "kind": "semantic",
          "function": "_ZN15IsaacRepentance4Room13GetFrameCountEv",
          "address": "0x00470b20",
          "immediate": 63900,
          "why": "该函数体内的取值常量 `0xf99c`（与 0x470B24 的 `movk #0x24` 合成 0x24F99C = `Game` 的帧计数）—— 它是『这条函数确实是 `Room::GetFrameCount`』的独立语义锚点（函数名 + 减数来源都指向同一件事）"
        }
      ]
    }
  ]
}
