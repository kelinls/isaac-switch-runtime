build/
deploy/
*.elf
*.nso
*.npdm

# 调试桩（gdbsym）那套构建的产物目录。它**不叫** `build/`，所以要单独写一行，
# 否则发布脚本"白名单下不许有未跟踪文件"的守卫会把发布拦下来（拦下来是对的）。
.gdbsym-build/
# 成本压缩期间的对照构建（同 `.gdbsym-build`，不叫 build/ 所以要单独写一行）。
.cost-build/

# 宿主 API 还原构建（tests 里按需生成再清理；曾经因为是"白名单下未跟踪目录"
# 被发布脚本的 `git add -A -- runtime` 带进公开仓库，223 个 .o/.d 文件）。
.apiart-build/
