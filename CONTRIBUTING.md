# 贡献与协作规范（CONTRIBUTING）

本项目是**开发测试版**，以"在真机上可验证、可回退"为第一原则。下面这些约定是踩过坑之后定下来的，
请照着来；它们比代码风格更重要。

---

## 一、三维验收基准（本项目对"API 兼容"的硬性要求）

我们把"运行时提供的 Lua API 与 PC 版行为是否一致"拆成**三个维度**，**每一条 API 三维都必须显式登记**，
**任何一维不满足都要打标记**（而不是默默放过）：

| 维度 | 含义 | 不合格的后果 | 判定方式 |
| --- | --- | --- | --- |
| **① 存在** | PC 有、我们有没有 | 模组里就是"调用 nil"；若在**没有 `pcall` 保护**的回调里，**整条描述/整个功能会消失** | 静态核对：PC 对照目录 × 运行时 `ApiCatalog`（`runtime/src/interfaces/lua/api_catalog.cpp`）。工具：`tools/eid_api_gap_report.py` |
| **② 语义** | 有实现，但返回值/行为是否与 PC 一致 | **不报错**，个别功能悄悄不对（例如曾经"套装计数恒 (0/3)"） | 以 PC 行为为基准逐条核对，核对结论与证据（真机读数/日志/行号）必须登记 |
| **③ 时机** | 回调/事件被派发的**时机**是否与 PC 一致 | 模组依赖"某回调一定在那一刻发生"的假设失效，表现为条件性失灵 | 只能在真机上对照：回调进入次数、派发点状态、Lua 错误文本等常驻读数 |

### 第 0 维：枚举面（前置，2026-09-16 补）

上面三条问的是"**这一条**接口对不对"，它们默认**接口清单本身是全的**。这个默认曾经是错的：

* 清单只覆盖**方法**（`obj:Method(`）—— 字段（PC 契约的 `Variables` 节，例如
  `ItemConfig_Item.Quality`）在模组里的写法是 `obj.Field`，既匹配不上方法提取的正则，
  也不在 `ApiCatalog`（目录只登记方法）⇒ **从来没被问过**。后果是"① 类缺口归零"这句话
  只对方法成立，而缺口本身是静默的（`EID` 读 `.Quality` 拿到 nil，被 `and desc.Quality`
  短路，品质图标直接不显示，不报错也没日志）。诊断与教训：
  `docs/问题与解决记录.md` 2026-09-16 续七、`docs/错误复盘.md` 2026-09-16。

因此补一条**前置维度**：

| 维度 | 要求 | 机器执行 |
| --- | --- | --- |
| **第 0 维 枚举面** | 三类接口面（**方法** / **字段** / 全局量与枚举）都必须有枚举来源，且每条都要有归属：已实现 / 已登记未实现 / 未登记缺口。"未登记缺口"**一律算失败** | `python3 tools/eid_api_gap_report.py --strict`（第 ⑤ 段）；台账 `tools/api_surface_ledger.json`；门禁 `runtime/tests/test_api_surface_ledger.py` |

配套约定：

* **人工分诊结论必须落盘**：工具只能说"模组读了它、我们没实现"；补/不补、为什么，写进
  `tools/api_surface_ledger.json` 并写明理由与证据位置。按名字匹配出来的**假阳性也要登记**
  （标 `wontfix` + 理由），否则下一轮还会有人重新分诊一遍。
* **汇报必须带口径**：说"缺口归零"时必须写明是"方法"还是"字段"，不能只给一个数字。
* 字段的偏移取证走既有配方：`tools/extract_struct_layout.py` → `tools/layout_tables/*.json`
  → `build_layout_table.py --verify`。

**登记口径**：
- 每条 API / 每个回调都必须有三项取值，且**带证据引用**（真机报告名、代码位置、对照来源）；
- **字段（`Variables`）与全局量同样要登记**：它们的载体是 `__index` / 静态字段表 / 布局表，
  处置写在 `tools/api_surface_ledger.json`（"第 0 维"一节），不能因为"目录里没有"就当不存在；
- "已知不一致"必须显式登记（原因 + 复现条件 + 计划），**不允许留空**；
- 门禁采用"**未登记 ⇒ 失败；已登记例外 ⇒ 允许**"，而不是"一律必须合格"——否则门禁会长期红着，等于没有。

---

## 二、测试纪律

1. **不许为了让自己的改动通过而去改测试。** 测试红了先判断性质：
   - **代码不对** ⇒ 改**代码**去满足测试（常态）；
   - **测试本身过期/过粗**（断言与实现的设计意图矛盾）⇒ **停下来问维护者**，不得自行改断言；
   - 判断"实现 vs 文档/设计"谁对：用 `git stash` 做对照 + 读实现处的注释，别猜。
2. **唯一例外**：契约测试里的**接口账本数字**（例如 `runtime/tests/contract/test_api_catalog.py` 的
   `assertEqual(checked, N, ...)`）是设计好的记账方式——每加一批 API 就 **+1**，并在上面补一行注释写明
   本批加了什么、为什么。
3. 改动 API 面（新增/删除/改签名）时，**必须同时更新 API Catalog 与账本数字**，否则契约测试会拦下。

---

## 三、真机取证纪律（凭据与设备）

1. **绝不提交设备凭据**：所有设备工具从 `--host/--user/--password`、环境变量（`ISAAC_DEVICE_*`）
   或本机私有文件 `tools/device.local.json`（已 gitignore）读取，统一入口 `tools/device_access.py`。
   提交前自查：`git grep -n -i "password\|passwd\|token" -- . ` 。
2. **FTP 不要常驻**：需要用的时候临时开、用完关掉再进游戏。后台常驻的 FTP（或任何常驻的 SD 占用者）
   会明显提高"游戏启动阶段崩溃"的概率。
3. **一次连接只做一件事**：设备 FTP 对连续快速连接敏感（会 `451` 或直接拒绝）。工具里已内置
   "一次连接一个动作 + 动作之间留 1.5 秒 + 失败等 5 秒单次重试"，不要自己写并发。
4. **写卡必须在游戏未运行时做**；**改过覆盖层（`romfs/`）之后的那次启动**最容易出问题，属预期：
   再启动一次；连续多次失败就把 `romfs_metadata.bin` 改名强制重建。
5. **判断"是不是同一个崩溃"要比"模块名 + 偏移"，绝不能比绝对地址**（ASLR 每次启动都换基址）。

---

## 四、"每有较大进展就更新一次"的约定（自动更新）

**规则**：每完成一次**较大进展**（bug 修复、新功能、重要诊断结论、发布包变更），**必须往 GitHub 更新一次**。
公开仓库：<https://github.com/kelinls/isaac-switch-runtime>（分支 `main`，由本机 `public` 分支同步）。

> ⚠️ 门禁失败（例如运行时的段预算超限）**不要**用 `SKIP_TESTS=1` 掩盖：先修问题，或在提交信息里写明为什么可以先发以及计划。

统一入口：

```bash
tools/publish_progress.sh "一句话标题" "做了什么 / 证据 / 影响 / 回退方式"
```

脚本会：跑门禁测试 → 按模板生成提交信息 → 提交 → 推送到公开仓库。
提交信息模板（四段，缺一不可，便于以后回溯）：

```
<类型>: <一句话标题>

做了什么：...
证据：...（真机报告名 / 测试名 / 读数）
影响：...（对玩家可见的变化、性能、风险）
回退：...（怎么撤销，越具体越好）
```

**不要**把下面这些提交上去：设备备份、崩溃报告、发布压缩包、第三方模组内容、设备凭据、
与本次改动无关的既有改动。

---

## 五、代码与文档

- 代码注释用中文，解释"**为什么这么做**"而不是"这行在干什么"；引用规则/事实时给出处（文件、行号、链接）。
- 设计发生变化时同步更新文档；没有对应文档就补一份。
- 每次任务前先看 `docs/问题与解决记录.md`（如仓库内含）；踩过的坑写进去，避免重复踩。
- 提交信息用中文，前缀按类型：`fix:` / `feat:` / `tools:` / `docs:` / `chore:` / `exp:`（实验性改动）。
